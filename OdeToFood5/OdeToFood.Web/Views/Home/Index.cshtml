@model IEnumerable<OdeToFood.Data.Models.Restaurant>

@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <h1>@Model.Count()</h1>
</div>

<ul>
    @foreach (var item in Model)
    {
        <li>@item.Name</li>
    }
</ul>